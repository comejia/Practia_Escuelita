rootProject.name = 'api-compliance-testing'

assignProperty('baseApiURL','https://jsonplaceholder.typicode.com/')
assignProperty('endpointEnding_GetUserURL','users/')

assignProperty('timeout' ,'25')


def assignProperty(String key, Object defaultValue){
    if(System.getProperty(key) == null){
        System.setProperty(key, defaultValue)
    }
}
